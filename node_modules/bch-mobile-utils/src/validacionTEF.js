const regexFirstLastName = /[^a-zA-Z0-9ñÑáéíóúÁÉÍÓÚ\s\.\-\/]/;

/*
* Valida caracteres de nombre y apellido.
*/
const validaFirstLastName = (text) => {
    return !regexFirstLastName.test(text);
};

export default {
    validaFirstLastName,
};
const checkBackView = (route, defaultView = 'Home') => {
    const { params } = route;
    if (params?.goBack) {
        return params.goBack;
    }
    return defaultView;
};

export default checkBackView;

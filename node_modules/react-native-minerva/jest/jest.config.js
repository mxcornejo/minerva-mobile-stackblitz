const EXTERNAL_MODULES = [
    'react-native',
    '@react-native-community',
    '@react-navigation',
    '@dynatrace/react-native-plugin',
    'native-base-shoutem-theme',
    '@codler',
    'native-base',
    '@react-native-async-storage',
    '@rainbow-me',
];
const BCH_MODULES = [
    'bch-assets',
    '@bch-mobile',
    'bch-mobile-constants',
    'bch-mobile-utils',
    'bch-mobile-crypto',
    'bch-mobile-header',
    'bch-mobile-caja-desafio',
];
const MODULES = [...EXTERNAL_MODULES, ...BCH_MODULES].join('|');

const config = {
    preset: 'react-native',
    rootDir: '../',
    setupFiles: ['<rootDir>/jest/jest.setup.js'],
    testResultsProcessor: 'jest-sonar-reporter',
    collectCoverage: true,
    transformIgnorePatterns: [`node_modules/(?!(jest-)?@?${MODULES})`],
    roots: ['<rootDir>/__tests__', '<rootDir>/__mocks__'],
};

module.exports = config;
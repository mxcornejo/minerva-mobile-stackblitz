import { renderHook } from '@testing-library/react-hooks';
import { useHeaderHeightStyle } from '../../src/index';

describe('Test hook useHeaderHeightStyle', () => {
    let wrapper;

    test('deberia useHeaderHeightStyle retornar estilos con altura del header', async () => {
        const { result } = renderHook(() => useHeaderHeightStyle(), {
            wrapper,
        });
        expect(result.current).toBeDefined();
    });
});

import { renderHook } from '@testing-library/react-hooks';
import { useTabBarHeightStyle } from '../../src/index';

describe('Test hook useTabBarHeightStyle', () => {
    let wrapper;

    test('deberia useHeaderHeightStyle retornar estilos con altura del tabBar', async () => {
        const { result } = renderHook(() => useTabBarHeightStyle(), {
            wrapper,
        });
        expect(result.current).toBeDefined();
    });
});

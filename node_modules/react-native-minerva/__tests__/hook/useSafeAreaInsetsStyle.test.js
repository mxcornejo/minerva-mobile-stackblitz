import { renderHook } from '@testing-library/react-hooks';
import { useSafeAreaInsetsStyle } from '../../src/index';

describe('Test hook useSafeAreaInsetsStyle', () => {
    let wrapper;

    test('deberia useHeaderHeightStyle retornar estilos con dimensiones del area segura', async () => {
        const { result } = renderHook(() => useSafeAreaInsetsStyle(), {
            wrapper,
        });
        expect(result.current).toBeDefined();
    });
});

import HeaderStyle from '../../../native-base/components/Header';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase HeaderStyle', () => {
    test('Debería estar definido', () => {
        expect(HeaderStyle(Minerva)).toBeDefined();
    });
});

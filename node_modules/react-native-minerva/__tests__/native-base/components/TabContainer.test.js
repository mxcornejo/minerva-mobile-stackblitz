import TabContainerStyle from '../../../native-base/components/TabContainer';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase TabContainerStyle', () => {
    test('Debería estar definido', () => {
        expect(TabContainerStyle(Minerva)).toBeDefined();
    });
});

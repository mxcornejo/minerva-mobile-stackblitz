import H2Style from '../../../native-base/components/H2';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase H2Style', () => {
    test('Debería estar definido', () => {
        expect(H2Style(Minerva)).toBeDefined();
    });
});

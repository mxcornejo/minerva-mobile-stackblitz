import BodyStyle from '../../../native-base/components/Body';

describe('Test NativeBase BodyStyle', () => {
    test('Debería estar definido', () => {
        expect(BodyStyle()).toBeDefined();
    });
});

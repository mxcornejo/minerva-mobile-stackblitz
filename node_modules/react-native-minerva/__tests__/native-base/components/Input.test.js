import InputStyle from '../../../native-base/components/Input';

describe('Test NativeBase InputStyle', () => {
    test('Debería estar definido', () => {
        expect(InputStyle()).toBeDefined();
    });
});

import BadgeStyle from '../../../native-base/components/Badge';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase BaggeStyle', () => {
    test('Debería estar definido', () => {
        expect(BadgeStyle(Minerva)).toBeDefined();
    });
});

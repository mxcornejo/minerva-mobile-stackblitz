import TabBarStyle from '../../../native-base/components/TabBar';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase TabBarStyle', () => {
    test('Debería estar definido', () => {
        expect(TabBarStyle(Minerva)).toBeDefined();
    });
});

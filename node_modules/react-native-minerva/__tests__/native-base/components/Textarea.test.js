import TextareaStyle from '../../../native-base/components/Textarea';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase TextareaStyle', () => {
    test('Debería estar definido', () => {
        expect(TextareaStyle(Minerva)).toBeDefined();
    });
});

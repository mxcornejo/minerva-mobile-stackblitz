import SubtitleStyle from '../../../native-base/components/Subtitle';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase SubtitleStyle', () => {
    test('Debería estar definido', () => {
        expect(SubtitleStyle(Minerva)).toBeDefined();
    });
});

import SeparatorStyle from '../../../native-base/components/Separator';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase SeparatorStyle', () => {
    test('Debería estar definido', () => {
        expect(SeparatorStyle(Minerva)).toBeDefined();
    });
});

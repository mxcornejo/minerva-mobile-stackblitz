import PickerIosStyle from '../../../native-base/components/Picker.ios';

describe('Test NativeBase PickerIosStyle', () => {
    test('Debería estar definido', () => {
        expect(PickerIosStyle()).toBeUndefined();
    });
});

import PickerStyle from '../../../native-base/components/Picker';

describe('Test NativeBase PickerStyle', () => {
    test('Debería estar definido', () => {
        expect(PickerStyle).toBeDefined();
    });
});

import ViewStyle from '../../../native-base/components/View';

describe('Test NativeBase ViewStyle', () => {
    test('Debería estar definido', () => {
        expect(ViewStyle()).toBeDefined();
    });
});

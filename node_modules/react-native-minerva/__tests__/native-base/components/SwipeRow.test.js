import SwipeRowStyle from '../../../native-base/components/SwipeRow';

describe('Test NativeBase SwipeRowStyle', () => {
    test('Debería estar definido', () => {
        expect(SwipeRowStyle()).toBeDefined();
    });
});

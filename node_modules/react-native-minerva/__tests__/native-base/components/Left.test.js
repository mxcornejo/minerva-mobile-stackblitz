import LeftStyle from '../../../native-base/components/Left';

describe('Test NativeBase LeftStyle', () => {
    test('Debería estar definido', () => {
        expect(LeftStyle()).toBeDefined();
    });
});

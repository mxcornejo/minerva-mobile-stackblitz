import ListItemStyle from '../../../native-base/components/ListItem';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase ListItemStyle', () => {
    test('Debería estar definido', () => {
        expect(ListItemStyle(Minerva)).toBeDefined();
    });
});

import InputGroupStyle from '../../../native-base/components/InputGroup';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase InputGroupStyle', () => {
    test('Debería estar definido', () => {
        expect(InputGroupStyle(Minerva)).toBeDefined();
    });
});

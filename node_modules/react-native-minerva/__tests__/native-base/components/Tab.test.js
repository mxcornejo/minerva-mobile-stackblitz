import TabStyle from '../../../native-base/components/Tab';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase TabStyle', () => {
    test('Debería estar definido', () => {
        expect(TabStyle(Minerva)).toBeDefined();
    });
});

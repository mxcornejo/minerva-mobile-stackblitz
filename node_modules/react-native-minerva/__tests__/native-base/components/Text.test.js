import TextStyle from '../../../native-base/components/Text';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase TextStyle', () => {
    test('Debería estar definido', () => {
        expect(TextStyle(Minerva)).toBeDefined();
    });
});

import ThumbnailStyle from '../../../native-base/components/Thumbnail';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase ThumbnailStyle', () => {
    test('Debería estar definido', () => {
        expect(ThumbnailStyle(Minerva)).toBeDefined();
    });
});

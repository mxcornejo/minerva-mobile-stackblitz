import TabHeadingStyle from '../../../native-base/components/TabHeading';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase TabHeadingStyle', () => {
    test('Debería estar definido', () => {
        expect(TabHeadingStyle(Minerva)).toBeDefined();
    });
});

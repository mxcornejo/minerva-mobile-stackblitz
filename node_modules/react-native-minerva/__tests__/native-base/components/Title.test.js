import TitleStyle from '../../../native-base/components/Title';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase TitleStyle', () => {
    test('Debería estar definido', () => {
        expect(TitleStyle(Minerva)).toBeDefined();
    });
});

import RadioStyle from '../../../native-base/components/Radio';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase RadioStyle', () => {
    test('Debería estar definido', () => {
        expect(RadioStyle(Minerva)).toBeDefined();
    });
});

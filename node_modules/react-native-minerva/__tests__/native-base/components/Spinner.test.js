import SpinnerStyle from '../../../native-base/components/Spinner';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase SpinnerStyle', () => {
    test('Debería estar definido', () => {
        expect(SpinnerStyle(Minerva)).toBeDefined();
    });
});

import PickerAndroidStyle from '../../../native-base/components/Picker.android';

describe('Test NativeBase PickerAndroidStyle', () => {
    test('Debería estar definido', () => {
        expect(PickerAndroidStyle()).toBeDefined();
    });
});

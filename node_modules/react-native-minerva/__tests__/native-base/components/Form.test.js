import FormStyle from '../../../native-base/components/Form';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase FormStyle', () => {
    test('Debería estar definido', () => {
        expect(FormStyle(Minerva)).toBeDefined();
    });
});

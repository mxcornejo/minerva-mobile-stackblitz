import H1Style from '../../../native-base/components/H1';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase H1Style', () => {
    test('Debería estar definido', () => {
        expect(H1Style(Minerva)).toBeDefined();
    });
});

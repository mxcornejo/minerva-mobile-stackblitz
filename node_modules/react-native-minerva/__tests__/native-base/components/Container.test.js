import ContainerStyle from '../../../native-base/components/Container';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase ContainerStyle', () => {
    test('Debería estar definido', () => {
        expect(ContainerStyle(Minerva)).toBeDefined();
    });
});

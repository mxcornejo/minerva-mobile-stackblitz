import IndexStyle from '../../../native-base/components/index';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase IndexStyle', () => {
    test('Debería estar definido', () => {
        expect(IndexStyle(Minerva)).toBeDefined();
    });
});

import ToastStyle from '../../../native-base/components/Toast';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase ToastStyle', () => {
    test('Debería estar definido', () => {
        expect(ToastStyle(Minerva)).toBeDefined();
    });
});

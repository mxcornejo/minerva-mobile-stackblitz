import IconStyle from '../../../native-base/components/Icon';

describe('Test NativeBase IconStyle', () => {
    test('Debería estar definido', () => {
        expect(IconStyle()).toBeDefined();
    });
});

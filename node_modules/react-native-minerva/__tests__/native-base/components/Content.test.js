import ContentStyle from '../../../native-base/components/Content';

describe('Test NativeBase ContentStyle', () => {
    test('Debería estar definido', () => {
        expect(ContentStyle()).toBeDefined();
    });
});

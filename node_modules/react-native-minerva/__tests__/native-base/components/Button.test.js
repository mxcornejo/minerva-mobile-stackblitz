import ButtonStyle from '../../../native-base/components/Button';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase ButtonStyle', () => {
    test('Debería estar definido', () => {
        expect(ButtonStyle(Minerva)).toBeDefined();
    });
});

import SwitchStyle from '../../../native-base/components/Switch';

describe('Test NativeBase SwitchStyle', () => {
    test('Debería estar definido', () => {
        expect(SwitchStyle()).toBeUndefined();
    });
});

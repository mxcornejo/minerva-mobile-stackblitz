import CardItemStyle from '../../../native-base/components/CardItem';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase CardItemStyle', () => {
    test('Debería estar definido', () => {
        expect(CardItemStyle(Minerva)).toBeDefined();
    });
});

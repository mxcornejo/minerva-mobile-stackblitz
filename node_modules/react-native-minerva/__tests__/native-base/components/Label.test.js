import LabelStyle from '../../../native-base/components/Label';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase LabelStyle', () => {
    test('Debería estar definido', () => {
        expect(LabelStyle(Minerva)).toBeDefined();
    });
});

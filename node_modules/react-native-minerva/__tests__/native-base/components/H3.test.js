import H3Style from '../../../native-base/components/H3';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase H3Style', () => {
    test('Debería estar definido', () => {
        expect(H3Style(Minerva)).toBeDefined();
    });
});

import CardStyle from '../../../native-base/components/Card';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase CardStyle', () => {
    test('Debería estar definido', () => {
        expect(CardStyle(Minerva)).toBeDefined();
    });
});

import SegmentStyle from '../../../native-base/components/Segment';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase SegmentStyle', () => {
    test('Debería estar definido', () => {
        expect(SegmentStyle(Minerva)).toBeDefined();
    });
});

import FabStyle from '../../../native-base/components//Fab';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase FabStyle', () => {
    test('Debería estar definido', () => {
        expect(FabStyle(Minerva)).toBeDefined();
    });
});

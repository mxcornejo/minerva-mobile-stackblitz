import FooterStyle from '../../../native-base/components/Footer';
import Minerva from '../../../native-base/variables/minerva';

describe('Test NativeBase FooterStyle', () => {
    test('Debería estar definido', () => {
        expect(FooterStyle(Minerva)).toBeDefined();
    });
});

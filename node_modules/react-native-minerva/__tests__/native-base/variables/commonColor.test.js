import { isString, isNumber } from 'lodash';
import CommonColorStyle from '../../../native-base/variables/commonColor';

describe('Test NativeBase CommonColorStyle', () => {
    test('Debería estar definido', () => {
        expect(CommonColorStyle).toBeDefined();
    });

    test('Debería obtener Getters', () => {
        expect(isString(CommonColorStyle.buttonPrimaryBg)).toBeTruthy();
        expect(isString(CommonColorStyle.buttonPrimaryColor)).toBeTruthy();
        expect(isString(CommonColorStyle.buttonInfoBg)).toBeTruthy();
        expect(isString(CommonColorStyle.buttonInfoColor)).toBeTruthy();
        expect(isString(CommonColorStyle.buttonSuccessBg)).toBeTruthy();
        expect(isString(CommonColorStyle.buttonSuccessColor)).toBeTruthy();
        expect(isString(CommonColorStyle.buttonDangerBg)).toBeTruthy();
        expect(isString(CommonColorStyle.buttonDangerColor)).toBeTruthy();
        expect(isString(CommonColorStyle.buttonWarningBg)).toBeTruthy();
        expect(isString(CommonColorStyle.buttonWarningColor)).toBeTruthy();
        expect(isNumber(CommonColorStyle.buttonTextSize)).toBeTruthy();
        expect(isNumber(CommonColorStyle.buttonTextSizeLarge)).toBeTruthy();
        expect(isNumber(CommonColorStyle.buttonTextSizeSmall)).toBeTruthy();
        expect(isNumber(CommonColorStyle.borderRadiusLarge)).toBeTruthy();
        expect(isNumber(CommonColorStyle.iconSizeLarge)).toBeTruthy();
        expect(isNumber(CommonColorStyle.iconSizeSmall)).toBeTruthy();
        expect(isNumber(CommonColorStyle.fontSizeH1)).toBeTruthy();
        expect(isNumber(CommonColorStyle.fontSizeH2)).toBeTruthy();
        expect(isNumber(CommonColorStyle.fontSizeH3)).toBeTruthy();
        expect(isString(CommonColorStyle.statusBarColor)).toBeTruthy();
        expect(isString(CommonColorStyle.darkenHeader)).toBeTruthy();
        expect(isString(CommonColorStyle.inputColor)).toBeTruthy();
        expect(isString(CommonColorStyle.inputColorPlaceholder)).toBeTruthy();
        expect(isString(CommonColorStyle.radioColor)).toBeTruthy();
        expect(isString(CommonColorStyle.defaultTextColor)).toBeTruthy();
    });
});

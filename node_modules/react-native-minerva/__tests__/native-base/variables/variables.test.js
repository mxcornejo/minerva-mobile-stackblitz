import { isString, isNumber } from 'lodash';
import VariablesStyle from '../../../native-base/variables/variables';

describe('Test NativeBase VariablesStyle', () => {
    test('Debería estar definido', () => {
        expect(VariablesStyle).toBeDefined();
    });

    test('Debería obtener Getters', () => {
        expect(isString(VariablesStyle.buttonPrimaryBg)).toBeTruthy();
        expect(isString(VariablesStyle.buttonPrimaryColor)).toBeTruthy();
        expect(isString(VariablesStyle.buttonInfoBg)).toBeTruthy();
        expect(isString(VariablesStyle.buttonInfoColor)).toBeTruthy();
        expect(isString(VariablesStyle.buttonSuccessBg)).toBeTruthy();
        expect(isString(VariablesStyle.buttonSuccessColor)).toBeTruthy();
        expect(isString(VariablesStyle.buttonDangerBg)).toBeTruthy();
        expect(isString(VariablesStyle.buttonDangerColor)).toBeTruthy();
        expect(isString(VariablesStyle.buttonWarningBg)).toBeTruthy();
        expect(isString(VariablesStyle.buttonWarningColor)).toBeTruthy();
        expect(isNumber(VariablesStyle.buttonTextSize)).toBeTruthy();
        expect(isNumber(VariablesStyle.buttonTextSizeLarge)).toBeTruthy();
        expect(isNumber(VariablesStyle.buttonTextSizeSmall)).toBeTruthy();
        expect(isNumber(VariablesStyle.borderRadiusLarge)).toBeTruthy();
        expect(isNumber(VariablesStyle.iconSizeLarge)).toBeTruthy();
        expect(isNumber(VariablesStyle.iconSizeSmall)).toBeTruthy();
        expect(isNumber(VariablesStyle.fontSizeH1)).toBeTruthy();
        expect(isNumber(VariablesStyle.fontSizeH2)).toBeTruthy();
        expect(isNumber(VariablesStyle.fontSizeH3)).toBeTruthy();
        expect(isString(VariablesStyle.statusBarColor)).toBeTruthy();
        expect(isString(VariablesStyle.darkenHeader)).toBeTruthy();
        expect(isString(VariablesStyle.inputColor)).toBeTruthy();
        expect(isString(VariablesStyle.inputColorPlaceholder)).toBeTruthy();
        expect(isString(VariablesStyle.radioColor)).toBeTruthy();
        expect(isString(VariablesStyle.defaultTextColor)).toBeTruthy();
    });
});

import { isString, isNumber } from 'lodash';
import MaterialStyle from '../../../native-base/variables/material';

describe('Test NativeBase MaterialStyle', () => {
    test('Debería estar definido', () => {
        expect(MaterialStyle).toBeDefined();
    });

    test('Debería obtener Getters', () => {
        expect(isString(MaterialStyle.buttonPrimaryBg)).toBeTruthy();
        expect(isString(MaterialStyle.buttonPrimaryColor)).toBeTruthy();
        expect(isString(MaterialStyle.buttonInfoBg)).toBeTruthy();
        expect(isString(MaterialStyle.buttonInfoColor)).toBeTruthy();
        expect(isString(MaterialStyle.buttonSuccessBg)).toBeTruthy();
        expect(isString(MaterialStyle.buttonSuccessColor)).toBeTruthy();
        expect(isString(MaterialStyle.buttonDangerBg)).toBeTruthy();
        expect(isString(MaterialStyle.buttonDangerColor)).toBeTruthy();
        expect(isString(MaterialStyle.buttonWarningBg)).toBeTruthy();
        expect(isString(MaterialStyle.buttonWarningColor)).toBeTruthy();
        expect(isNumber(MaterialStyle.buttonTextSize)).toBeTruthy();
        expect(isNumber(MaterialStyle.buttonTextSizeLarge)).toBeTruthy();
        expect(isNumber(MaterialStyle.buttonTextSizeSmall)).toBeTruthy();
        expect(isNumber(MaterialStyle.borderRadiusLarge)).toBeTruthy();
        expect(isNumber(MaterialStyle.iconSizeLarge)).toBeTruthy();
        expect(isNumber(MaterialStyle.iconSizeSmall)).toBeTruthy();
        expect(isNumber(MaterialStyle.fontSizeH1)).toBeTruthy();
        expect(isNumber(MaterialStyle.fontSizeH2)).toBeTruthy();
        expect(isNumber(MaterialStyle.fontSizeH3)).toBeTruthy();
        expect(isString(MaterialStyle.statusBarColor)).toBeTruthy();
        expect(isString(MaterialStyle.darkenHeader)).toBeTruthy();
        expect(isString(MaterialStyle.inputColor)).toBeTruthy();
        expect(isString(MaterialStyle.inputColorPlaceholder)).toBeTruthy();
        expect(isString(MaterialStyle.radioColor)).toBeTruthy();
        expect(isString(MaterialStyle.defaultTextColor)).toBeTruthy();
    });
});

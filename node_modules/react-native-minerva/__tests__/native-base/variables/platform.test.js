import { isString, isNumber } from 'lodash';
import PlataformStyle from '../../../native-base/variables/platform';

describe('Test NativeBase PlataformStyle', () => {
    test('Debería estar definido', () => {
        expect(PlataformStyle).toBeDefined();
    });

    test('Debería obtener Getters', () => {
        expect(isString(PlataformStyle.buttonPrimaryBg)).toBeTruthy();
        expect(isString(PlataformStyle.buttonPrimaryColor)).toBeTruthy();
        expect(isString(PlataformStyle.buttonInfoBg)).toBeTruthy();
        expect(isString(PlataformStyle.buttonInfoColor)).toBeTruthy();
        expect(isString(PlataformStyle.buttonSuccessBg)).toBeTruthy();
        expect(isString(PlataformStyle.buttonSuccessColor)).toBeTruthy();
        expect(isString(PlataformStyle.buttonDangerBg)).toBeTruthy();
        expect(isString(PlataformStyle.buttonDangerColor)).toBeTruthy();
        expect(isString(PlataformStyle.buttonWarningBg)).toBeTruthy();
        expect(isString(PlataformStyle.buttonWarningColor)).toBeTruthy();
        expect(isNumber(PlataformStyle.buttonTextSize)).toBeTruthy();
        expect(isNumber(PlataformStyle.buttonTextSizeLarge)).toBeTruthy();
        expect(isNumber(PlataformStyle.buttonTextSizeSmall)).toBeTruthy();
        expect(isNumber(PlataformStyle.borderRadiusLarge)).toBeTruthy();
        expect(isNumber(PlataformStyle.iconSizeLarge)).toBeTruthy();
        expect(isNumber(PlataformStyle.iconSizeSmall)).toBeTruthy();
        expect(isNumber(PlataformStyle.fontSizeH1)).toBeTruthy();
        expect(isNumber(PlataformStyle.fontSizeH2)).toBeTruthy();
        expect(isNumber(PlataformStyle.fontSizeH3)).toBeTruthy();
        expect(isString(PlataformStyle.statusBarColor)).toBeTruthy();
        expect(isString(PlataformStyle.darkenHeader)).toBeTruthy();
        expect(isString(PlataformStyle.inputColor)).toBeTruthy();
        expect(isString(PlataformStyle.inputColorPlaceholder)).toBeTruthy();
        expect(isString(PlataformStyle.radioColor)).toBeTruthy();
        expect(isString(PlataformStyle.defaultTextColor)).toBeTruthy();
    });
});

import React from 'react';
import { render } from '@testing-library/react-native';
import ContentGradient from '../../../src/components/contentGradient/ContentGradient';
import { Text } from '../../../src';

describe('Test componente <ContentGradient />', () => {
    test('Debería renderizar correctamente', () => {
        const { toJSON } = render(
            <ContentGradient
                renderContentGradient={() => <Text>Contenido superior</Text>}
                renderContent={() => <Text>Contenido inferior</Text>}
            />,
        );
        expect(toJSON()).toMatchSnapshot();
    });
});

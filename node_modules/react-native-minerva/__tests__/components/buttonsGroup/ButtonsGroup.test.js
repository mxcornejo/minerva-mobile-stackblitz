import React from 'react';
import { render } from '@testing-library/react-native';
import ButtonsGroup from '../../../src/components/buttonsGroup/ButtonsGroup';
import { HelperStyle } from '../../../src/index';

describe('Test componente <ButtonsGroup />', () => {
    const props = {
        items: [
            { title: 'primary', props: { primary: true } },
            { title: 'secondary', props: { secondary: true } },
        ],
    };
    test('Debería renderizar correctamente', () => {
        const { items } = props;
        const { toJSON } = render(
            <ButtonsGroup style={HelperStyle.mb_4} items={items} />,
        );
        expect(toJSON()).toMatchSnapshot();
    });
});

import React from 'react';
import { render } from '@testing-library/react-native';
import { PasswordChecklist } from '../../../src/index';

describe('Test componente <PasswordChecklist />', () => {
    const data = [
        {
            value: 'Id eu dolore mollit nostrud exercitation do consectetur Lorem.',
            check: true,
        },
        {
            value: 'Cupidatat enim velit consequat id sunt sunt enim deserunt anim minim ea pariatur dolor.',
            check: false,
        },
    ];
    test('Debería renderizar correctamente', () => {
        const { toJSON } = render(<PasswordChecklist data={data} />);
        expect(toJSON()).toMatchSnapshot();
    });
});

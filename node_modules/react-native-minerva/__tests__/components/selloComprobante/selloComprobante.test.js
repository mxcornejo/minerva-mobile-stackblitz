import React from 'react';
import { render } from '@testing-library/react-native';
import SelloComprobante from '../../../src/components/selloComprobante/selloComprobante';

describe('Test componente <SelloComprobante />', () => {
    test('Debería renderizar correctamente', () => {
        const { toJSON } = render(<SelloComprobante />);
        expect(toJSON()).toMatchSnapshot();
    });
});

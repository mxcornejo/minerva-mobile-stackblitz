import React from 'react';
import { render } from '@testing-library/react-native';
import { ListBulleted, Text } from '../../../src/index';

describe('Test componente <ListBulleted />', () => {
    const customRender = () => <Text>Right</Text>;
    const items = [
        {
            renderRight: customRender,
        },
        {
            value: 'In laboris nulla non minim excepteur proident exercitation ut commodo labore.',
        },
        {
            renderLeft: customRender,
        },
    ];
    test('Debería renderizar correctamente', () => {
        const { toJSON } = render(<ListBulleted items={items} />);
        expect(toJSON()).toMatchSnapshot();
    });
});

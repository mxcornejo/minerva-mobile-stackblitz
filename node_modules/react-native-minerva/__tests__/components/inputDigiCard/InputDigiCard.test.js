import React from 'react';
import { render } from '@testing-library/react-native';
import { InputDigiCard } from '../../../src/index';

describe('Test componente <InputDigiCard />', () => {
    const mockFn = jest.fn();
    const keyboardType = 'number-pad';
    const propsInput = [
        {
            label: 'A1',
            onChangeText: mockFn,
            keyboardType,
            accessibilityLabel: 'coordenada A1',
            testID: 'coordenada A1',
        },
        {
            label: 'B2',
            onChangeText: mockFn,
            keyboardType,
            accessibilityLabel: 'coordenada B3',
            testID: 'coordenada B3',
        },
        {
            label: 'C3',
            onChangeText: mockFn,
            order: 3,
            keyboardType,
            accessibilityLabel: 'coordenada C3',
            testID: 'coordenada C3',
            returnKeyType: 'done',
            onSubmitEditing: mockFn,
        },
    ];
    test('Debería renderizar correctamente', () => {
        const { toJSON } = render(<InputDigiCard propsInput={propsInput} />);
        expect(toJSON()).toMatchSnapshot();
    });
});

import React from 'react';
import { render } from '@testing-library/react-native';
import FadeInUpView from '../../../src/components/fadeInUpView/fadeInUpView';
import { Text } from '../../../src';

describe('Test componente <FadeInUpView />', () => {
    test('Debería renderizar correctamente', () => {
        const { toJSON } = render(
            <FadeInUpView>
                <Text>lorem</Text>
            </FadeInUpView>,
        );
        expect(toJSON()).toMatchSnapshot();
    });
});

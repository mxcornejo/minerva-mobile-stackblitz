import Style from '../../../src/components/switch/style';

jest.mock('../../../src/constants/platform', () => ({
    android: true,
    ios: false,
}));

describe('Test Style Switch', () => {
    test('Debería estar definido los estilos de Switch para Platform IOS', () => {
        expect(Style).toBeDefined();
    });
});

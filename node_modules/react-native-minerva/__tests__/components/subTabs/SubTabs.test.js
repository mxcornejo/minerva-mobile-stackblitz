import React from 'react';
import { render } from '@testing-library/react-native';
import { SubTabs, Text } from '../../../src/index';

describe('Test componente <SubTabs />', () => {
    const buttonsHeader = [{ title: 'SubTab 1' }, { title: 'SubTab 2' }];
    const renderContentSegment = () => <Text>content</Text>;
    test('Debería renderizar correctamente', () => {
        const { toJSON } = render(
            <SubTabs
                selected={jest.fn(() => null)}
                itemActive={1}
                buttonsHeader={buttonsHeader}
                renderContentSegment={renderContentSegment}
            />,
        );
        expect(toJSON()).toMatchSnapshot();
    });
});

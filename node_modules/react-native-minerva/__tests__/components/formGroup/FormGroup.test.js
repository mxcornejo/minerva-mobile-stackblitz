import React from 'react';
import { render } from '@testing-library/react-native';
import FormGroup from '../../../src/components/formGroup/FormGroup';
import { Icon, Input } from '../../../src';

const renderIcon = () => <Icon type="Ionicons" name="eye" />;

describe('Test componente <FormGroup />', () => {
    test('Debería renderizar correctamente', () => {
        const { toJSON } = render(
            <FormGroup renderRight={renderIcon()} renderLeft={renderIcon()}>
                <Input pass label="Password" />
                <Input pass label="Repita Password" />
            </FormGroup>,
        );
        expect(toJSON()).toMatchSnapshot();
    });
});

import React from 'react';
import { render } from '@testing-library/react-native';
import BounceIndicator from '../../../src/components/bounceIndicator/BounceIndicator';

describe('Test componente <BounceIndicator />', () => {
    test('Debería renderizar correctamente', () => {
        const { toJSON } = render(<BounceIndicator />);
        expect(toJSON()).toMatchSnapshot();
    });
});

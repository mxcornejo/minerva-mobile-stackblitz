import React from 'react';
import { render } from '@testing-library/react-native';
import IconCircleFill from '../../../src/components/iconCircleFill/IconCircleFill';

describe('Test componente <IconCircleFill />', () => {
    test('Debería renderizar correctamente', () => {
        const { toJSON } = render(<IconCircleFill />);
        expect(toJSON()).toMatchSnapshot();
    });
});

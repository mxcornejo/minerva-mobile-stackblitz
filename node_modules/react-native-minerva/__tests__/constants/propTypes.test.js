import { DEFAULT_TYPES } from '../../src/constants/propTypes';

describe('Test constantes propTypes', () => {
    test('Deberia existir DEFAULT_TYPES.func', () => {
        expect(DEFAULT_TYPES.func()).toBe(null);
    });
});

import { StyleSheet } from 'react-native';
import { HelperStyle, Spacing } from '../../index';

const style = StyleSheet.create({
    imageStyle: {
        width: Spacing.spacer_5,
        height: Spacing.spacer_5,
        alignSelf: 'center',
        ...HelperStyle.mh_2,
    },
});

export default style;

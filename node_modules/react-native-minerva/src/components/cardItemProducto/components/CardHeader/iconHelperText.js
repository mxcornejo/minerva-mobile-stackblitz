import React, { memo } from 'react';
import PropTypes from 'prop-types';
import { Icon, HelperStyle } from '../../../../index';

/**
 * Componente icono de ayuda.
 *
 * @component
 */
const IconHelperText = (props) => {
    const { style } = props;
    return (
        <Icon
            name="icon-bch-alert-info"
            {...props}
            style={[
                HelperStyle.fs_3,
                HelperStyle.ml_1,
                HelperStyle.as_c,
                style,
            ]}
        />
    );
};

IconHelperText.propTypes = {
    style: PropTypes.oneOfType([PropTypes.object, PropTypes.array]),
};
IconHelperText.defaultProps = {
    style: {},
};

export default memo(IconHelperText);

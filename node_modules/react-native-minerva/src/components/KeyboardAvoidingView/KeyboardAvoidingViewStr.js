import { getStatusBarHeight } from '../../index';

const KeyboardAvoidingViewStr = {
    keyboardVerticalOffset: {
        header: getStatusBarHeight + 44,
    },
    testID: 'testIDKeyboardAvoidingView',
    behavior: {
        padding: 'padding',
        height: 'height',
        position: 'position',
    },
};

export default KeyboardAvoidingViewStr;

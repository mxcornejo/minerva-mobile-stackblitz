import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    listItemLeft: {
        flex: 0.8,
        zIndex: 1,
    },
    listItemRight: {
        flex: 1.2,
    },
});

export default style;

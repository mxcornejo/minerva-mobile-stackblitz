import { StyleSheet } from 'react-native';
import { Spacing, Scale } from '../../index';

const style = StyleSheet.create({
    circle: {
        position: 'absolute',
        right: 0,
        height: Spacing.spacer_1,
        width: Spacing.spacer_1,
        borderRadius: Spacing.radius_1,
    },
    circleContainer: {
        width: Scale(20),
        height: Spacing.spacer_5,
        justifyContent: 'center',
        position: 'absolute',
        top: -Scale(10),
        zIndex: 1,
    },
});

export default style;

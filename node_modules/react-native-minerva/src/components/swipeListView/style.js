import { StyleSheet } from 'react-native';
import { Spacing, ColorsBCH } from '../../index';

const Style = StyleSheet.create({
    rowBack: {
        alignItems: 'center',
        backgroundColor: ColorsBCH.white,
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'space-between',
        paddingLeft: Spacing.spacer_4,
    },
    backBtn: {
        alignItems: 'center',
        bottom: 0,
        justifyContent: 'center',
        position: 'absolute',
        top: 0,
    },
    firstBackRightBtn: {
        borderTopLeftRadius: Spacing.radius_2,
        borderBottomLeftRadius: Spacing.radius_2,
    },
    firstBackLeftBtn: {
        borderTopRightRadius: Spacing.radius_2,
        borderBottomRightRadius: Spacing.radius_2,
    },
    backgroundItem: {
        position: 'absolute',
        width: '100%',
        height: '100%',
        zIndex: -1,
        backgroundColor: ColorsBCH.white,
    },
});

export default Style;

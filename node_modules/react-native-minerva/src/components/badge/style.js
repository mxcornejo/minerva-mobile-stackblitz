import { StyleSheet } from 'react-native';
import { Scale } from '../../index';

const Style = (HelperStyle, size) =>
    StyleSheet.create({
        iconTextStyle: {
            ...HelperStyle.as_c,
            ...HelperStyle.mr_1,
        },
        iconClose: {
            fontSize: Scale(6),
            ...HelperStyle.white,
        },
        customSize: {
            width: Scale(size),
            minWidth: Scale(size),
            height: Scale(size),
            borderRadius: Scale(size) / 2,
        },
    });
export default Style;

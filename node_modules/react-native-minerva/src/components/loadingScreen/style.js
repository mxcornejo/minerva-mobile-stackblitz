import { StyleSheet } from 'react-native';
import { getHeaderHeight, dimensions, Base, Scale } from '../../index';

const style = StyleSheet.create({
    imageBackground: {
        width: '100%',
        height: '100%',
        position: 'absolute',
        top: 0,
        zIndex: 3,
    },
    imageStyle: {
        width: '100%',
        height: '100%',
        resizeMode: 'cover',
    },
    ripple: {
        position: 'absolute',
        height: Scale(10),
        alignSelf: 'center',
    },
    rippleSquare: {
        width: dimensions.fullWidth * 0.13,
        top: getHeaderHeight + Base,
    },
    rippleCircle: {
        bottom: 1,
        width: Scale(10),
    },
    containerLoading: {
        zIndex: 3,
        flex: 1,
    },
});

export default style;

import { StyleSheet } from 'react-native';
import { Spacing } from '../../index';

const style = StyleSheet.create({
    cardItemHeader: {
        paddingTop: Spacing.spacer_3,
        paddingRight: Spacing.spacer_4,
        paddingBottom: Spacing.spacer_2,
        paddingLeft: Spacing.spacer_4,
    },
});

export default style;

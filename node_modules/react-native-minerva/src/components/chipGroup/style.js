import { StyleSheet } from 'react-native';

const Style = (HelperStyle) =>
    StyleSheet.create({
        horizontalStyle: {
            ...HelperStyle.flx_row,
            ...HelperStyle.flx_wrap,
        },
    });

export default Style;

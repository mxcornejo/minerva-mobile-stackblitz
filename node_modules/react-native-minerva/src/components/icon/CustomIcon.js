import { createIconSetFromIcoMoon } from 'react-native-vector-icons';
import Assets from 'bch-assets';

const { icoMoonConfig } = Assets;

export default createIconSetFromIcoMoon(
    icoMoonConfig,
    'icomoon',
    'icomoon.ttf',
);

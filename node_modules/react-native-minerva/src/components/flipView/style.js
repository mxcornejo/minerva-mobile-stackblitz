import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    flipView: {
        position: 'absolute',
    },
    flipBack: {
        opacity: 0,
    },
});

export default style;

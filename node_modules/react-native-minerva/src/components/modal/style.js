import { StyleSheet } from 'react-native';
import { Spacing, Scale } from '../../index';

const style = StyleSheet.create({
    modal: {
        justifyContent: 'flex-end',
        margin: 0,
    },
    modalContent: {
        borderTopLeftRadius: Spacing.radius_3,
        borderTopRightRadius: Spacing.radius_3,
    },
    modalCloseBar: {
        width: Scale(64),
        height: Scale(6),
        borderRadius: Spacing.spacer_1,
        marginTop: Spacing.spacer_2,
        marginBottom: 0,
        alignSelf: 'center',
    },
});

export default style;

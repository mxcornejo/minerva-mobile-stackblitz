import { StyleSheet } from 'react-native';
import { Scale } from '../../../../index';

const Style = ({ HelperStyle }) =>
    StyleSheet.create({
        getOptionContainer: (disabled) => [
            HelperStyle.flx_row,
            HelperStyle.jc_center,
            HelperStyle.ai_c,
            disabled && HelperStyle.o_30,
        ],
        optionImage: {
            width: Scale(30),
            height: Scale(20),
        },
        optionIcon: [HelperStyle.fs_1, HelperStyle.ml_2, HelperStyle.mr_2],
        optionText: [HelperStyle.mb_0, HelperStyle.gray_dark],
    });

export default Style;

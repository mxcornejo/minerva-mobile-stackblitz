import { StyleSheet } from 'react-native';
import { ColorsBCH } from '../../index';

const style = StyleSheet.create({
    inputSearchLight: {
        backgroundColor: ColorsBCH.white,
        borderTopWidth: 1,
        borderRightWidth: 1,
        borderLeftWidth: 1,
        borderBottomWidth: 1,
    },
});

export default style;

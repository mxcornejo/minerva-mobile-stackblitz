import { StyleSheet } from 'react-native';
import { Scale, Spacing } from '../../../index';

const style = StyleSheet.create({
    contentCollapsedItem: {
        width: '33.3%',
    },
    collapsibleMain: {
        marginTop: -Scale(16),
    },
    modalCloseBar: {
        width: Scale(28),
        height: Scale(3),
        borderRadius: Spacing.spacer_1,
        marginTop: Spacing.spacer_1,
        marginBottom: Spacing.spacer_1,
    },
});

export default style;

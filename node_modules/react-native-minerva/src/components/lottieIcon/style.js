import { StyleSheet } from 'react-native';
import { Spacing, Scale } from '../../index';

const style = StyleSheet.create({
    small: {
        height: Spacing.spacer_7,
        width: Spacing.spacer_7,
    },
    medium: {
        height: Scale(60),
        width: Scale(60),
    },
    large: {
        height: Scale(120),
        width: Scale(120),
    },
});

export default style;

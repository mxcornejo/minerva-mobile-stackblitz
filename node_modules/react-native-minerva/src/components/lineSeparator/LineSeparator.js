import React, { memo } from 'react';
import PropTypes from 'prop-types';
import { View, HelperStyle } from '../../index';
import Style from './style';

/**
 * Componente línea separadora.
 *
 * @component
 */
const LineSeparator = ({ style, testID }) => (
    <View
        testID={testID}
        style={[Style.lineSeparator, HelperStyle.bc_gray_lighter, style]}
    />
);

LineSeparator.propTypes = {
    /**
     * Estilos.
     */
    style: PropTypes.oneOfType([PropTypes.object, PropTypes.array]),
    /**
     * Identificador para pruebas.
     */
    testID: PropTypes.string,
};
LineSeparator.defaultProps = {
    style: {},
    testID: 'IDLineSeparator',
};

export default memo(LineSeparator);

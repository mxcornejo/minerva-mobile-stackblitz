import { StyleSheet } from 'react-native';
import { Spacing, ColorsBCH } from '../../index';

const Style = StyleSheet.create({
    scrollableTab: {
        backgroundColor: ColorsBCH.transparent,
        minHeight: null,
        height: null,
    },
    underlineStyle: {
        borderRadius: Spacing.radius_3,
    },
});

export default Style;

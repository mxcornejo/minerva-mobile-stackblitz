import { StyleSheet } from 'react-native';
import { Spacing, Fonts, Scale, ColorsBCH } from '../../index';

const Style = (props) => {
    const { HelperStyle } = props;
    return StyleSheet.create({
        BotonesNumericos: {
            fontSize: Spacing.type_5,
            fontFamily: Fonts.fontFamilyBold,
            color: HelperStyle.white.color,
            lineHeight: Scale(62),
            width: Scale(62),
            height: Scale(62),
            textAlign: 'center',
            marginBottom: 0,
        },
        TouchableContainer: {
            borderRadius: Scale(62) / 2,
        },
        hiddenText: {
            position: 'absolute',
            color: ColorsBCH.transparent,
        },
    });
};

export default Style;

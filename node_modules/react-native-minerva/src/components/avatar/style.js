import { StyleSheet } from 'react-native';
import { Spacing, Scale } from '../../index';

const style = StyleSheet.create({
    materialIndicator: {
        position: 'absolute',
        top: -Scale(2),
        alignSelf: 'center',
    },
    iconCloseContainerLarge: {
        top: Scale(8),
        right: Scale(8),
    },
    iconCloseContainer: {
        position: 'absolute',
        right: -Scale(3),
        top: -Spacing.spacer_1,
    },
    avatarContainer: {
        justifyContent: 'center',
        alignItems: 'center',
    },
    avatarFlipViewContainer: {
        zIndex: 2,
    },
});

export default style;

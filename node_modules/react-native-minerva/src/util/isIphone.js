import { Platform, Dimensions } from 'react-native';

const { width, height } = Dimensions.get('window');
const platform = Platform.OS;

const isIphone = {
    Big: platform === 'ios' && (height >= 812 || width >= 812),
    Small: platform === 'ios' && height <= 568,
};

export default isIphone;

// @flow
import { Scale, minerva } from '../../src/index';

export default (variables /* : * */ = minerva) => ({
    flexDirection: 'row',
    justifyContent: 'center',
    alignSelf: 'stretch',
    minHeight: Scale(48),
    shadowColor: null,
    shadowOffset: null,
    shadowRadius: null,
    shadowOpacity: null,
    borderBottomWidth: variables.borderWidth,
    borderColor: variables.topTabBarBorderColor,
    overflow: 'hidden',
});

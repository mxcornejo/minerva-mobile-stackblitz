// @flow
import { Spacing, minerva } from '../../src/index';

export default (variables /* : * */ = minerva) => ({
    fontSize: Spacing.type_2,
    color: variables.textColor,
    '.focused': {
        width: 0,
    },
});

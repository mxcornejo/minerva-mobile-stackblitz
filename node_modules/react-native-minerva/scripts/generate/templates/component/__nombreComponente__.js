import React, { memo } from 'react';
import PropTypes from 'prop-types';
import Style from './style';
import labels from './labels';

const propTypes = {};

const defaultProps = {};

const __nombreComponente__ = (props) => {
    return (
        // Codigo de component __nombreComponente__
    );
};

__nombreComponente__.propTypes = propTypes;
__nombreComponente__.defaultProps = defaultProps;

export default memo(__nombreComponente__);

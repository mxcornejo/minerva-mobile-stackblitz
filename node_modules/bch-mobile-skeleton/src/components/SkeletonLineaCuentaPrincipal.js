import React from 'react';
import {
    HelperStyle,
    View,
    Grid,
    Col,
    Skeleton,
} from 'react-native-minerva';

/**
 * Renderiza Skeleton linea de credito de Cuenta Principal
 */
const SkeletonLineaCuentaPrincipal = () => (
    <View style={[HelperStyle.flx_row, HelperStyle.as_c, HelperStyle.jc_c, HelperStyle.pt_0, HelperStyle.pb_4]}>
        <Grid>
            <Col size={2} />
            <Col size={6}>
                <Skeleton transparent numberOfItems={1} />
            </Col>
            <Col size={2} />
        </Grid>
    </View>
);

export default SkeletonLineaCuentaPrincipal;

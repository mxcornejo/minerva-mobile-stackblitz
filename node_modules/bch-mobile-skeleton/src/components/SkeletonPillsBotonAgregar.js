import React from 'react';
import {
    HelperStyle,
    Grid,
    Col,
    Skeleton,
} from 'react-native-minerva';

const pillsStyle = { height: 44, borderRadius: 64 };

/**
 * Renderiza Skeleton Pills (SubTab) y botón circular agregar
 */
const SkeletonPillsBotonAgregar = () => (
    <Grid>
        <Col size={7} style={[HelperStyle.ph_4, HelperStyle.pr_5]}>
            <Skeleton numberOfItems={1} style={pillsStyle} />
        </Col>
        <Col size={1} style={HelperStyle.pt_1}>
            <Skeleton numberOfItems={1} type="media" size={44} />
        </Col>
    </Grid>
);

export default SkeletonPillsBotonAgregar;

import React, { memo } from 'react';
import {
    Card,
    CardItem,
    Skeleton,
    View,
    HelperStyle,
} from 'react-native-minerva';

const numberOfItems = [1, 2, 3];

const SkeletonDispositivosRegistrados = () => (
    <View>
        {numberOfItems.map(() => (
            <Card cardList>
                <CardItem avatar row style={HelperStyle.flx_i}>
                    <Skeleton numberOfItems={1} firstLineWidth="70%" border type="Paragraph" />
                </CardItem>
            </Card>
        ))}
    </View>
);

export default memo(SkeletonDispositivosRegistrados);
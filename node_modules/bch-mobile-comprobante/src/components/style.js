import { StyleSheet, Dimensions } from 'react-native';
import { Scale } from 'react-native-minerva';

const { width } = Dimensions.get('window');

const style = StyleSheet.create({
    marginTopStyle: {
        marginTop: -Scale(10),
    },
    imageStyle: {
        width: Scale(width),
        height: Scale(30),
        transform: [{ scale: 1.6 }],
    },
    processingIconStyle: {
        fontSize: Scale(52),
    },
});

export default style;

import React from 'react';
import { render } from '@testing-library/react-native';
import HeaderGradientTitle from '../../src/components/HeaderGradientTitle';

describe('Test para componente HeaderGradientTitle', () => {
    test('deberia renderizar correctamente', () => {
        const { toJSON } = render(
            <HeaderGradientTitle>Title</HeaderGradientTitle>,
        );
        expect(toJSON()).toMatchSnapshot();
    });
});
